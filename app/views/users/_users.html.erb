<% if users.any? %>
  <ul>
    <% users.each do |user| %>
      <li>
        <%= user.name %>
        <p><%= link_to 'View profile', user_path(user) %></p>
        <p><%= link_to 'Logout', logout_path, method: :delete %></p>
      </li>
    <% end %>
  </ul>
<% end %>
